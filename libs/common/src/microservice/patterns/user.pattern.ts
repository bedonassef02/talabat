export enum UserPattern {
  CREATE = 'user.create',
  FIND_ALL = 'user.find_all',
  FIND_ONE = 'user.find_one',
  FIND_BY_EMAIL = 'user.find_by_email',
  UPDATE = 'user.update',
  REMOVE = 'user.remove',
}
